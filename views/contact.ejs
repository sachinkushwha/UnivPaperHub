<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Contact Us | AGCPYQP</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Poppins', sans-serif;
    }
  </style>
</head>

<body class="bg-gradient-to-br from-blue-100 via-purple-100 to-pink-100 min-h-screen">
<%-include('partials/nave')%>
  <div class="max-w-3xl mx-auto mt-12 p-8 bg-white rounded-3xl shadow-2xl border border-purple-200">
    <h1 class="text-4xl font-bold mb-4 text-center text-purple-700">📬 Contact Us</h1>
    <p class="text-gray-600 text-center mb-8 text-lg">Have any questions or feedback? We'd love to hear from you!</p>

    <form action="/contact" method="post" class="space-y-6">
      <div>
        <label for="name" class="block text-sm font-semibold text-gray-700">👤 Your Name</label>
        <input type="text" id="name" name="userName" required
          class="mt-1 block w-full p-3 border-2 border-purple-300 rounded-xl focus:ring-2 focus:ring-purple-500 focus:outline-none transition-all">
      </div>

      <div>
        <label for="email" class="block text-sm font-semibold text-gray-700">📧 Email Address</label>
        <input type="email" id="email" name="email" required
          class="mt-1 block w-full p-3 border-2 border-purple-300 rounded-xl focus:ring-2 focus:ring-purple-500 focus:outline-none transition-all">
      </div>

      <div>
        <label for="message" class="block text-sm font-semibold text-gray-700">💬 Your Message</label>
        <textarea id="message" name="msg" rows="5" required
          class="mt-1 block w-full p-3 border-2 border-purple-300 rounded-xl focus:ring-2 focus:ring-purple-500 focus:outline-none transition-all"></textarea>
      </div>

      <div class="text-center">
        <button type="submit"
          class="bg-gradient-to-r from-purple-600 to-pink-500 text-white px-8 py-3 rounded-full shadow-lg hover:scale-105 transition duration-300 ease-in-out">
          🚀 Send Message
        </button>
      </div>
    </form>
  </div>

 <%-include('partials/footer')%>
</body>

</html>
