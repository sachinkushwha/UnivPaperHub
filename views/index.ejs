<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Previous Year Papers</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-100">
    <%-include('partials/nave')%>
        <div class="container  mx-auto p-6">
            <h1 class="text-3xl font-bold text-center mb-6">Previous Year Question Papers</h1>

            <% semester.forEach((sem)=>{%>
                <h2 class="text-2xl font-semibold mb-4"><%=sem%></h2>
                <div class="flex flex-wrap gap-6">
                    <% qpdata.forEach((data=>{ %>
                        <% if(data.semester===sem){%>
                        <div class="mb-6">
                            <div class="bg-white p-4 rounded-lg shadow-md">
                                <h2 class="text-xl font-semibold">Subject: <%=data.subject%>
                                </h2>
                                <p class="text-gray-600">Year: <%=data.year%>
                                </p>
                                <div class="mt-2">
                                    <a href="/download/<%=data._id%>" download="<%=data.originalname%>"
                                        class="bg-blue-500 text-white px-4 py-2 rounded-lg mr-2">Download</a>
                                    <a href="/viewpaper/<%=data._id%>" target="_blank"
                                        class="bg-green-500 text-white px-4 py-2 rounded-lg">📷 View</a>
                                </div>
                            </div>
                        </div>
                        <%}%>
                        <%}))%>
                </div>
            <%})%>
                <!-- semester 2 ke liye -->
                <!-- <% let sem2=qpdata.filter((data)=>data.semester==='semester2');%>
                    <h2 class="text-2xl font-semibold mb-4">semester2</h2>
                    <div class="flex flex-wrap gap-6">
                        <% sem2.forEach((data=>{ %>
                            <div class="mb-6">
                                <div class="bg-white p-4 rounded-lg shadow-md">
                                    <h2 class="text-xl font-semibold">Subject: <%=data.subject%>
                                    </h2>
                                    <p class="text-gray-600">Year: <%=data.year%>
                                    </p>
                                    <div class="mt-2">
                                        <a href="papers/sem1_maths_2023.pdf" download
                                            class="bg-blue-500 text-white px-4 py-2 rounded-lg mr-2">Download</a>
                                        <a href="papers/sem1_maths_2023.pdf" target="_blank"
                                            class="bg-green-500 text-white px-4 py-2 rounded-lg">📷 View</a>
                                    </div>
                                </div>
                            </div>
                            <%}))%>
                    </div> -->

        </div>
</body>

</html>