<!DOCTYPE html>
<html lang="en">

<head>
    <meta name="description"
        content="AGCPYQP helps you prepare for exams by practicing previous year question papers online.">
    <meta name="keywords" content="AGCPYQP, previous year question papers, practice papers, online test">
    <meta name="author" content="sachin kushwaha">
    <meta name="google-site-verification" content="0AZXvz5XhCMKaRtmkmv1LppDr507uOzxnpZ946MgIUQ" />
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Previous Year Papers | PYQP</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-100">
    <%-include('../partials/nave')%>
        <div class="container  mx-auto p-6">
            <h1 class="text-2xl text-white font-bold text-center mb-6">Your Question Paper List</h1>

            <% semester.forEach((sem)=>{%>
                <h2 class="text-2xl font-semibold mb-4 text-white">
                    <%=sem%>
                </h2>
                <div class="flex flex-wrap gap-6">
                    <% qpdata.forEach((data=>{ %>
                        <% if(data.semester===sem){%>
                            <div class="mb-6">
                                <div class="bg-white p-4 rounded-lg shadow-md">
                                    <h2 class="text-xl font-semibold">Subject: <%=data.subject%>
                                    </h2>
                                    <p class="text-gray-600">PaperType: <%=data.papertype%>
                                    </p>
                                    <p class="text-gray-600">Year: <%=data.year%>
                                    </p>
                                    <div class="mt-2 flex gap-2">
                                        <form action="/delete/<%=data._id%>" method="post">
                                            <button type="submit"
                                                onclick="return confirm('Are you sure you want to delete this paper?')"
                                                class="bg-red-500 text-white px-4 py-2 rounded-lg">🗑️ Delete</button>
                                        </form>
                                        <a href="/viewpaper/<%=data._id%>" target="_blank"
                                            class="bg-green-500 text-white px-4 py-2 rounded-lg">📷 View</a>
                                    </div>
                                    
                                </div>
                            </div>
                            <%}%>
                                <%}))%>
                </div>
                <%})%>
                   

        </div>
        <%-include('../partials/footer')%>
</body>

</html>